# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Often committed, but can be ignored in some workflows. Add back if needed.
yarn.lock # Often committed, but can be ignored in some workflows. Add back if needed.
pnpm-lock.yaml # Often committed, but can be ignored in some workflows. Add back if needed.

# Build output
dist/
build/
out/
*.vsix
*.js.map
*.d.ts.map

# TypeScript cache
*.tsbuildinfo

# Logs
logs/
*.log
*.logs

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-project
*.sublime-workspace

# Environment variables
.env
.env.*
!.env.example

# Jest cache
/coverage

# Temporary files
*.tmp
*.swp
*~

# Cline rules
.clinerules

# Memory Bank (tracked by its own git repo)
memory_docs/
traceform/browser-extension/zip/traceform-browser-extension-0.1.1.zip
