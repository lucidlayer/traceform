{
  "name": "traceform-workspace",
  "version": "1.1.0",
  "private": true,
  "description": "Root workspace for the Traceform project",
  "license": "BUSL-1.1",
  "workspaces": [
    "babel-plugin-traceform",
    "browser-extension",
    "cli-onboard",
    "shared",
    "vscode-extension"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "format": "npm run format -ws --if-present",
    "clean": "npm run clean -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.5" 
  }
}
